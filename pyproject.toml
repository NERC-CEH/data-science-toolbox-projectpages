[project]
name = "data-science-toolbox"
version = "0.1.0"
description = "Python environment for rendering the data science toolbox"
authors = [{name = "UK Centre for Ecology & Hydrology"}]
license = "MIT"
license-files = ["LICENSE.md"]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter-book>=2.1.6",
    "pyyaml>=6.0.3",
]
